{
  "name": "napp",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "set -e & cd ../contract && ./do-build.sh",
    "postbuild": "cp ../contract/target/wasm32-unknown-unknown/release/*.wasm res/",
    "start": "npm link && clear && napp help",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "preferGlobal": true,
  "author": "Deveus",
  "license": "ISC",
  "bin": "./src/commands.js",
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^8.3.0",
    "inquirer": "^8.2.0",
    "near-api-js": "^0.43.1",
    "near-seed-phrase": "^0.2.0"
  }
}
